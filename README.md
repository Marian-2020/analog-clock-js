# analog-clock-js
Analog clock project built with HTML5 Canvas and JavaScript. The application renders the clock face, numbers and moving hands, using drawing functions and timed updates to simulate an animated analog clock directly in the browser.
